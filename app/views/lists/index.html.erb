<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('cl_image_tag(denise-jans-mxhWy1td-BE-unsplash_syxvxp,
      width: 400, height: 300, crop: :fill)');">
  <div class="container">
    <h1>🎥 Cinéma 🎬</h1>
    <p>the watch list</p>
  </div>
</div>

<div><header></header></div>
<h1>List of lists</h1>
  <% @lists.each do |list| %>
    <%= link_to list_path(list) do%>
      <% if list.photo.attached? %>
        <div class="card-product">
          <%= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %>
          <div class="card-product-infos">
            <h2><%= list.name %></h2>
            <%= link_to "Edit", edit_list_path(list), class:"fas fa-pencil-alt" %>
          </div>
        </div>
      <% else %>
        <div class="card-product">
          <div class="card-product-infos">
            <h2><%= list.name %></h2>
            <%= link_to "Edit", edit_list_path(list), class:"fas fa-pencil-alt" %>
          </div>
        </div>
      <% end %>
    <% end %>
  <% end %>

<%= link_to 'Create new list', new_list_path, class:"btn btn-outline-primary" %>
